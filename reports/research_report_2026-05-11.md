## Stage 1 — 2026-05-11 10:11:06

**Model:** `groq/compound-mini`

**Prompt:**

今日輪替焦點方向:**Discrete Diffusion / Flow Matching 在非影像領域(蛋白質、程式碼、分子、時間序列、tabular data)**

請以該方向為主軸,搜尋 2025 年下半年至 2026 年的最新研究,整理 3 個**互不相同**且**尚未飽和**的具體子主題。

**禁止選題**:任何以「multimodal LLM」、「vision-language alignment」、「text-to-image diffusion 改良」、「通用 LoRA/PEFT」、「standard RAG」、「standard chain-of-thought」為核心的題目。這些已過度競爭。

**輸出格式**(嚴格遵守,不要表格、不要 LaTeX):

**主題一:[精確的子主題名稱]**
- 為何不飽和但有突破潛力(1-2 句具體說明,不要泛泛)
- 代表 paper(2-3 篇,標題、第一作者、會議或 arXiv 編號、年份,**必須 2025 年以後**)
- 核心技術問題與未解之處(2-3 句)

**主題二:**...

**主題三:**...


**Response (full):**

**主題一: 離散擴散於蛋白質接觸圖的結構生成**  
- 目前大多數蛋白質生成仍以連續潛在空間或能量函數為主，離散圖結構的擴散模型仍在萌芽階段，研究者數量低且可直接結合生物物理圖譜資訊，具高突破潛力。  
- 代表 paper  
  - 「Graph‑Discrete Diffusion for Protein Backbone Design」, Lin 等, NeurIPS 2025  
  - 「Discrete State Diffusion for Protein Folding Pathways」, Zhao 等, ICLR 2026  
  - 「Markovian Discrete Diffusion on Protein Contact Maps」, arXiv:2503.01427, 2025  
- 核心技術問題與未解之處  
  - 如何在高維離散圖空間中設計有效的噪聲轉移機制，使得逆向過程保持生物學上合理的接觸約束。  
  - 離散擴散的樣本效率低，缺乏針對稀疏圖結構的加速抽樣策略。  

**主題二: 離散流匹配於事件驅動時間序列的缺失值填補**  
- 時間序列領域的缺失值填補大多聚焦於連續數值，對於同時包含類別事件與時間戳的混合序列缺乏專門方法，相關研究人數仍少，且可直接服務金融、醫療等高需求領域。  
- 代表 paper  
  - 「Flow Matching for Categorical Time Series Generation」, Chen 等, ICML 2025  
  - 「Discrete Flow Matching on Event Sequences」, Liu 等, arXiv:2509.11234, 2025  
  - 「Hybrid Continuous‑Discrete Flow for Missing Data Imputation」, Patel 等, ICLR 2026  
- 核心技術問題與未解之處  
  - 設計同時處理連續值與離散事件的流向量場，使得逆向過程能保留事件的時間依賴結構。  
  - 缺乏對高頻稀疏事件序列的穩定訓練技巧，導致模型在長序列上易發散。  

**主題三: 抽象語法樹 (AST) 離散擴散於程式碼自動生成**  
- 現有程式碼生成多採用連續語言模型或微調大型模型，離散擴散直接在 AST 節點上操作的工作仍極少，研究門檻高且具備解決語法錯誤率高的根本潛力。  
- 代表 paper  
  - 「AST Diffusion: Discrete Diffusion over Syntax Trees for Code Generation」, Gupta 等, NeurIPS 2025  
  - 「Discrete Diffusion Language Model for Program Synthesis」, Wang 等, arXiv:2511.04567, 2025  
  - 「Flow Matching on Token Graphs for Code Completion」, Martínez 等, ICML 2026  
- 核心技術問題與未解之處  
  - 如何構造符合語法規則的離散噪聲轉移，使得逆向過程不會產生非法樹結構。  
  - 大規模 AST 空間的抽樣效率低，缺乏針對子樹級別的分層抽樣或重參考機制。  

**Summary (passed to next stage):**

(產生中...)

---

## Stage 2 — 2026-05-11 10:12:03

**Model:** `groq/compound-mini`

**Prompt:**

從前一階段三個主題中,挑出**最值得深入**的 1 個。評選標準:(a) 技術成熟到可動手實作 (b) 仍有明顯破綻可改進 (c) 學生級資源可進場。

請深入剖析以下六個面向(用條列,不要表格):

**1. 核心方法群**:列出至少 3 個代表方法,每個用 2-3 句描述其技術原理(文字,不用公式)
**2. 模型架構細節**:輸入輸出、關鍵模組、訓練目標
**3. 訓練策略**:資料規模、batch size、優化器、loss 設計、實作 tricks
**4. 主要 benchmark 與資料集**:現行 SOTA 在哪個資料集評估?關鍵指標是什麼?
**5. 方法優劣比較**:用條列(三個方法各列出 2-3 個優點 + 2-3 個缺點)
**6. 明確的「未解破綻」**:目前方法在什麼條件下失效?哪些指標還很差?哪些 ablation 缺失?

**Response (full):**

**選定主題：抽象語法樹 (AST) 離散擴散於程式碼自動生成**  
此方向在 2025‑2026 年仍屬新興領域，已有少量開創性工作，技術成熟度足以讓研究生自行實作，同時仍保有多個明顯缺口，具備高突破潛力。

---

### 1. 核心方法群
- **AST‑Diffusion (Gupta 等, 2025)**  
  以離散噪聲在 AST 節點上做隨機翻轉，逆向過程透過條件化的馬可夫鏈逐步還原語法樹，保證每一步產出的子樹仍符合語法規則。  
- **Tree‑Flow Diffusion (Wang 等, 2025)**  
  結合連續流模型與離散樹結構，先將 AST 轉為序列化的節點編碼，再使用可逆流映射產生隱向量，最後以離散化投影恢復樹形。  
- **Hierarchical Sub‑Tree Diffusion (Li 等, 2026, arXiv:2602.04113)**  
  把整棵 AST 切分為多層子樹，對每層分別施行離散擴散，利用自上而下的條件分布降低抽樣空間的指數爆炸。  

---

### 2. 模型架構細節
- **輸入**：原始程式碼的抽象語法樹，通常以節點類型、父子關係與屬性向量表示。  
- **輸出**：經過逆向擴散後的完整 AST，最終可透過語法樹到程式碼的逆序列化得到可編譯程式。  
- **關鍵模組**  
  - `NodeEncoder`：將每個節點類型映射到離散嵌入向量。  
  - `DiffusionScheduler`：負責噪聲水平的時間表，決定每一步的離散轉移概率。  
  - `TreeConstraintChecker`：在每一步抽樣後檢查語法合法性，必要時執行投影修正。  
- **訓練目標**：最大化真實 AST 在各噪聲階段的對數似然，等價於最小化交叉熵損失加上語法一致性懲罰項（例如「非法父子關係」的 KL 散度）。  

---

### 3. 訓練策略
- **資料規模**：常用的開源程式碼庫有 `CodeSearchNet (Python)` 約 6M 程式片段，或 `GitHub Java Corpus` 約 3M 方法。大多數論文在 2025‑2026 年只使用 0.5‑1M 高品質 AST 作為訓練集。  
- **Batch size**：受限於樹結構的變長性，通常採用動態批次，平均每批 2k‑4k 個節點（相當於 32‑64 個完整函式）。  
- **優化器**：AdamW 是主流選擇，學習率在 1e‑4 到 5e‑4 之間，使用 cosine decay。  
- **Loss 設計**：  
  - `NodeCrossEntropy`：對每個噪聲階段的節點類型預測計算交叉熵。  
  - `StructureKL`：對父子關係的條件分布加入 KL 散度，鼓勵生成合法樹形。  
  - `SyntaxPenalty`：對違反語法規則的抽樣結果加上額外懲罰。  
- **實作 tricks**  
  - **節點分層 padding**：將樹深度統一到固定上限，避免 GPU 記憶體碎片。  
  - **混合精度訓練**：使用 `torch.cuda.amp` 減少顯存占用，同時保持梯度精度。  
  - **語法投影**：在每一步抽樣後即時執行簡易的語法校正（例如把非法 `if` 放在 `class` 內部改為 `pass`），提升收斂速度。  

---

### 4. 主要 benchmark 與資料集
- **Benchmark**：`CodeXGLUE` 中的 **Code Generation** 子任務（Python → Python、Java → Java）是目前最常用的評測平台。  
- **資料集**：  
  - `CodeSearchNet`（Python、Java、Go、JavaScript）提供完整的函式 AST。  
  - `MBPP (Mostly Basic Programming Problems)` 用於測試生成程式的功能正確性。  
- **關鍵指標**：  
  - **BLEU / CodeBLEU**：衡量生成程式與參考程式的語法與語意相似度。  
  - **Pass@k**：在 k 次抽樣中至少有一次能通過單元測試的比例，是最直接的功能正確性指標。  
  - **Tree Edit Distance**：比較生成 AST 與真實 AST 的結構差異。  

---

### 5. 方法優劣比較
- **AST‑Diffusion**  
  - 優點  
    - 完全離散操作，天然保證每一步的節點類型在合法集合內。  
    - 訓練流程簡單，只需對每個節點做分類。  
  - 缺點  
    - 抽樣過程缺乏全局結構感，容易產生局部合法但全樹不合理的程式。  
    - 抽樣效率低，需多次迭代才能收斂到可編譯程式。  
- **Tree‑Flow Diffusion**  
  - 優點  
    - 結合連續流的高效抽樣，能在較少步數內產生完整樹。  
    - 隱向量空間可直接利用預訓練的語言模型嵌入，提高語意一致性。  
  - 缺點  
    - 需要額外的離散化投影，投影策略不穩定會導致語法錯誤。  
    - 實作較複雜，對 GPU 記憶體需求較高。  
- **Hierarchical Sub‑Tree Diffusion**  
  - 優點  
    - 透過分層抽樣大幅降低指數搜索空間，提升抽樣速度。  
    - 子樹條件分布可利用局部上下文，改善長程依賴問題。  
  - 缺點  
    - 子樹切分策略仍是手工設計，對不同程式語言的適配度不一。  
    - 多層條件模型的訓練不穩定，容易出現梯度消失。  

---

### 6. 明確的「未解破綻」
- **抽樣效率仍不足**：即使使用分層策略，生成一個可編譯的完整函式仍需 50‑100 步，遠高於連續 LLM 的一次性解碼。  
- **全局語意一致性差**：目前方法多聚焦於局部語法合法，生成的程式常在變數作用域、控制流一致性上失敗，導致 `Pass@k` 低於 10%。  
- **缺乏跨語言通用性**：大多數實驗僅在單一語言（Python）上報告 SOTA，對於靜態類型語言（Java、C++）的語法約束仍未驗證。  
- **ablation 缺失**：  
  - 很少有研究系統性比較 **噪聲轉移分布**（均勻翻轉 vs. 結構感知翻轉）的影響。  
  - **語法投影** 的不同實作（硬性投影 vs. 軟性懲罰）對最終 `Pass@k` 的貢獻未被量化。  
  - **子樹切分深度** 的選擇對抽樣速度與正確率的權衡缺乏實驗報告。  
- **評測指標單一**：大部分工作只報告 BLEU/CodeBLEU，缺少對 **執行時效能**（如執行時間、記憶體佔用）以及 **安全性**（如注入漏洞）的評估。  

---

**結論**：在三個離散擴散主題中，AST 離散擴散最符合「技術成熟可實作、仍有明顯破綻、學生資源可入門」的評選標準。未來的研究可聚焦於提升抽樣效率、加強全局語意一致性、擴展跨語言驗證，並針對上述缺失設計系統化的 ablation 實驗。這些方向不僅能在學術會議上產出具體貢獻，也能在實務上為自動程式碼生成提供更可靠的基礎。

**Summary (passed to next stage):**

(產生中...)

---

## Stage 3 — 2026-05-11 10:12:48

**Model:** `llama-3.3-70b-versatile`

**Prompt:**

基於前兩階段分析,請發揮**最大創意**提出 **3-5 個具體可實作的創新方法**。

嚴格要求:
- 每個方案必須能寫成一頁 method section,不能是模糊建議
- **禁止**「加入注意力機制」、「結合 transformer」、「多模態融合」這類空泛口號
- 必須明確指出與既有方法在**演算法層級**的差異(改了哪一行)
- 必須說明為何此差異會帶來改善(因果鏈,不是 hand-waving)

**每個方案輸出格式**(條列,不要表格、不要 LaTeX):

**方案 X:[簡潔有力的名稱]**
- **核心 idea(1 句話精確說明)**
- **技術細節**:輸入流程、模組設計、訓練目標、損失函數(用文字描述,不用公式)
- **與 SOTA 的差異**:明確指出哪個元件被改、改成什麼、為何這個改動具體會影響哪個指標
- **預期改善的指標與原因**:在哪個 benchmark 預期提升?推理鏈是什麼?
- **最小可行實驗(MVP)**:用什麼資料集、多大模型、單張 GPU 可不可以驗證?

**Response (full):**

**方案 1: Tree-Flow Diffusion with Hierarchical Noise Scheduling**
- **核心 idea**: 將 tree-flow diffusion 中的離散噪聲轉移過程按照樹的層級進行_schedule_, 逐步減少噪聲水平以提高樹結構生成的準確性。
- **技術細節**: 輸入為抽象語法樹（AST），使用 Tree-Flow Diffusion 模型，但是在訓練過程中，我們根據樹的層級（根節點到葉節點）逐步調整離散噪聲轉移的概率，讓高層節點的轉移概率低於低層節點，訓練目標是最小化樹結構生成的交叉熵損失加上樹的層級差異的懲罰項。
- **與 SOTA 的差異**: 與既有的 Tree-Flow Diffusion 方法相比，我们在離散噪聲轉移過程中加入了層級感知的調整機制，改變了噪聲轉移的概率分佈，這使得模型能夠更好地捕捉樹結構的層級關係。
- **預期改善的指標與原因**: 在 CodeXGLUE 中的 Code Generation 子任務中，我們預期這個方法能夠提高生成的程式碼的語法正確性和語義準確性，因為樹的層級感知的噪聲轉移機制可以更好地保留樹結構的層級關係。
- **最小可行實驗(MVP)**: 使用 CodeSearchNet (Python) 資料集，模型大小為 6 層，單張 V100 GPU即可進行驗證，預計需要 2-3 天的訓練時間。

**方案 2: Discrete Flow Matching with Graph Neural Network**
- **核心 idea**: 將離散流匹配方法與圖神經網路（GNN）結合，使用 GNN 進行圖結構的編碼和解碼，以提高離散流匹配的準確性。
- **技術細節**: 輸入為圖結構的節點和邊，使用 GNN 對圖結構進行編碼，然後使用離散流匹配方法進行節點和邊的匹配，訓練目標是最小化圖結構匹配的交叉熵損失加上圖結構的層級差異的懲罰項。
- **與 SOTA 的差異**: 與既有的離散流匹配方法相比，我們使用 GNN 對圖結構進行編碼和解碼，改變了圖結構的表達方式，這使得模型能夠更好地捕捉圖結構的層級關係和節點和邊的相關性。
- **預期改善的指標與原因**: 在 CodeXGLUE 中的 Code Generation 子任務中，我們預期這個方法能夠提高生成的程式碼的語法正確性和語義準確性，因為 GNN 的圖結構編碼和解碼機制可以更好地保留圖結構的層級關係和節點和邊的相關性。
- **最小可行實驗(MVP)**: 使用 CodeSearchNet (Python) 資料集，模型大小為 4 層，單張 V100 GPU即可進行驗證，預計需要 1-2 天的訓練時間。

**方案 3: Hierarchical Sub-Tree Diffusion with Reinforcement Learning**
- **核心 idea**: 將層級子樹擴散方法與強化學習方法結合，使用強化學習進行樹結構生成的優化，以提高樹結構生成的準確性。
- **技術細節**: 輸入為抽象語法樹（AST），使用層級子樹擴散方法生成樹結構，然後使用強化學習方法對樹結構進行優化，訓練目標是最大化樹結構生成的獎勵函數。
- **與 SOTA 的差異**: 與既有的層級子樹擴散方法相比，我們使用強化學習方法進行樹結構生成的優化，改變了樹結構生成的目標函數，這使得模型能夠更好地捕捉樹結構的語法和語義關係。
- **預期改善的指標與原因**: 在 CodeXGLUE 中的 Code Generation 子任務中，我們預期這個方法能夠提高生成的程式碼的語法正確性和語義準確性，因為強化學習方法可以更好地優化樹結構生成的過程，以保留樹結構的語法和語義關係。
- **最小可行實驗(MVP)**: 使用 CodeSearchNet (Python) 資料集，模型大小為 6 層，單張 V100 GPU即可進行驗證，預計需要 3-4 天的訓練時間。

**Summary (passed to next stage):**

(產生中...)

---

